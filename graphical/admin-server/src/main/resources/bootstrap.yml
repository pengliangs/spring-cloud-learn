server:
  port: 9010
spring:
  application:
    name: admin-server

eureka:
  instance:
    prefer-ip-address: true
    hostname: admin-server
  client:
    service-url:
      defaultZone: http://eureka:8761/eureka/